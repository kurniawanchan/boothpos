org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official

# research.md R7 — arm64-v8a only for the initial build. Real tablets in
# the field are ARM; armeabi-v7a is a deferred follow-up only if device
# coverage during rollout actually requires it (not committed to up front
# on a guess — see plan.md's Complexity Tracking framing).
BOOTHPOS_TARGET_ABI=arm64-v8a
