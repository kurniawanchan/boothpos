meta {
  name: 6 - Ekspor Laporan Penjualan
  type: http
  seq: 6
}

get {
  url: {{base_url}}/reports/sales/export?event_id={{event_id}}
  auth: none
}

headers {
  Authorization: Bearer {{owner_token}}
}

tests {
  test("berkas xlsx laporan penjualan terunduh", function () {
    expect(res.status).to.equal(200);
  });
}
