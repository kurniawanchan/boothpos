meta { name: 4 - Cashier Akses Laporan Profit (negative)
  type: http
  seq: 4
}
get {
  url: {{base_url}}/reports/profit?event_id={{event_id}}
  auth: none
}
headers { Authorization: Bearer {{cashier_token}} }
tests {
  test("ditolak 403", function () {
    expect(res.status).to.equal(403);
  });
}
