meta {
  name: 5 - Ekspor Artist
  type: http
  seq: 5
}

get {
  url: {{base_url}}/exports/artists
  auth: none
}

headers {
  Authorization: Bearer {{owner_token}}
}

tests {
  test("berkas xlsx artist terunduh, kolomnya sama dengan template impor (round-trip)", function () {
    expect(res.status).to.equal(200);
  });
}
