meta {
  name: 3 - Login Salah Password (negative)
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "owner",
    "password": "password_salah"
  }
}

tests {
  test("ditolak dengan 401", function () {
    expect(res.status).to.equal(401);
  });
}
